<%= form_for(@menu_to_role) do |f| %>
  <% if @menu_to_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_to_role.errors.count, "error") %> prohibited this menu_to_role from being saved:</h2>

      <ul>
      <% @menu_to_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role_id %><br>
    <%= f.text_field :role_id %>
  </div>
  <div class="field">
    <%= f.label :menu_id %><br>
    <%= f.text_field :menu_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
